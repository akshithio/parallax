// Unpacked development builds keep verbose diagnostics on. The release packager
// replaces this file in the archive with the same flag set to false.
Object.defineProperty(globalThis, '__parallaxBuildConfig', {
  value: Object.freeze({ debug: true }),
  configurable: false,
  enumerable: false,
  writable: false,
});
