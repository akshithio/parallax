{
  "name": "nix",
  "version": "0.1.1",
  "description": "Nix desktop workspace, browser bridge, and website",
  "author": "Akshith Chowdary",
  "private": true,
  "packageManager": "pnpm@10.8.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  },
  "scripts": {
    "dev": "pnpm --filter nix-desktop dev",
    "dev:website": "pnpm --filter nix-website dev",
    "test": "pnpm --filter nix-extension test && pnpm --filter nix-desktop test && pnpm --filter nix-website test",
    "verify:versions": "node scripts/verify-versions.js",
    "verify": "pnpm run verify:versions && pnpm --filter nix-extension verify && pnpm --filter nix-desktop verify && pnpm --filter nix-website verify",
    "package:extension": "pnpm --filter nix-extension package"
  }
}
